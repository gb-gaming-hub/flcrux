'use strict'

const NAME = 'flatlineEX'
const VERSION = `${NAME}.0.1.0+vbomb`

const config = {
  channels: ['#flatline_test'],
  server: 'irc.quakenet.org',
  botName: NAME,
  userName: NAME,
  version: VERSION
};

module.exports = config;
